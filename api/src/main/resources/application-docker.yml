spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/statement_db
    username: postgres
    password: password
  jpa:
    hibernate:
      ddl-auto: update

app:
  aws:
    region: us-east-1
    s3:
      bucket: customer-statements
      endpoint: http://localstack:4566
